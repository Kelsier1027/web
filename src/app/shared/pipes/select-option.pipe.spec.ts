import { SelectOptionPipe } from './select-option.pipe';

describe('SelectOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
