import { GeneralComboPricePipe } from './general-combo-price.pipe';

describe('GeneralComboPricePipe', () => {
  it('create an instance', () => {
    const pipe = new GeneralComboPricePipe();
    expect(pipe).toBeTruthy();
  });
});
