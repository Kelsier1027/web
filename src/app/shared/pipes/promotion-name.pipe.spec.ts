import { PromotionNamePipe } from './promotion-name.pipe';

describe('PromotionNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PromotionNamePipe();
    expect(pipe).toBeTruthy();
  });
});
