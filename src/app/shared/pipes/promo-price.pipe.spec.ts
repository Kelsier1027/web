import { PromoPricePipe } from './promo-price.pipe';

describe('PromoPricePipe', () => {
  it('create an instance', () => {
    const pipe = new PromoPricePipe();
    expect(pipe).toBeTruthy();
  });
});
