import { PromotionOptionPipe } from './promotion-option.pipe';

describe('PromotionOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new PromotionOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
