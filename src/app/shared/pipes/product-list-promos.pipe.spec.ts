import { ProductListPromosPipe } from './product-list-promos.pipe';

describe('ProductListPromosPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductListPromosPipe();
    expect(pipe).toBeTruthy();
  });
});
