import { AveragePricePipe } from './average-price.pipe';

describe('AverageUnitPricePipeTsPipe', () => {
  it('create an instance', () => {
    const pipe = new AveragePricePipe();
    expect(pipe).toBeTruthy();
  });
});
